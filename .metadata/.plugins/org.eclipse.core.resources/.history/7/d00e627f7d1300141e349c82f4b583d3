Ti.include("JSON.js");

var currentWindow = Ti.UI.currentWindow;

//Views
var View = Ti.UI.createView({
	backgroundImage:"Images/Menu/Black.jpg",
	height: "121%"
});

var textView = Ti.UI.createView({
	backgroundColor: "#white",
	height: 65,
	top: 0	
});

var Border = Ti.UI.createView({
	backgroundColor: "black",
	height: 0,
	top: textView.height + textView.top	
});

var pat = Ti.UI.createImageView({
image:"Images/Miami/Pat.png",
zIndex: 1,
bottom:"0%"
});

//Labels
var Text = Ti.UI.createLabel({
	text: Miami[0],
	font: {fontSize: 15, fontFamily: "Arial"},
	color: "red",
	top: textView.height + Border.top + 40,
	left: 15,
	right: 15,
	zIndex: 2
});

//Button
var button = Titanium.UI.createButton({
   title: 'Click',
   top: 10,
   width: 100,
   height: 50,
   color: " red",
   backgroundImage: "Images/Button/Background.png"
});

//Functions//
var loadNext = function(){
	var loadfile = Ti.UI.createWindow({
		title:"1988­-2003: Early years in Miami",
		url:"Heat1.js",
		nav: currentWindow.nav
	});
	currentWindow.nav.openWindow(loadNext);
};

currentWindow.add(View,Text,button,pat);
